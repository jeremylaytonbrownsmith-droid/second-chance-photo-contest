# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files — never commit real secrets
.env
.env.local
.env.*.local

# testing
/coverage
/playwright-report/
/test-results/

# local-storage-provider uploads — runtime output, not source; the local
# storage provider writes here on `npm run dev` / `npm run seed`
/public/uploads/

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
